# Fixtures

Deterministic inputs used by tests, replay, and benchmarks.

Guidelines
- Keep fixtures stable and versioned (prefer adding over mutating).
- Keep fixtures small enough for fast CI.
- Avoid network dependencies; fixtures must run offline.

