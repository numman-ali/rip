# OpenResponses Stream Fixtures

This folder mirrors the canonical OpenResponses stream fixtures in:
- `crates/rip-provider-openresponses/fixtures/openresponses/`

Used by
- Replay and benchmark harnesses that want a single fixture root.

