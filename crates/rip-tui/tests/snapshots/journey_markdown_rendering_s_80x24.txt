- · rip · -                                                       idle  ttft 200
›  Summarize the deploy plan in markdown.

◉  ## Deploy plan
   Steps in order:
   1. Cut a release branch
   2. Run smoke tests
   3. Flip the feature flag
   │ Rollback is one flag flip.
   ```sh
   rip deploy --env=prod
   ```
   ────
   See the runbook ↗ for details.








▎ · Continue the thread
   ? help   ⌃K command   ⌥M models   click top row
